git_url("git@github.com:nabeelanasaarsitaosaire/ios-certificates.git")

# Use the correct parameter name
git_branch("main")

storage_mode("git")

type("appstore")        # default type when not overridden

app_identifier([
  "com.saoirse.epi"
])

username("samvictormichael@gmail.com")
team_id("BYQUNNQSQH")

# Optional but important
keychain_name("login.keychain")
keychain_password(ENV["KEYCHAIN_PASSWORD"])

readonly(false) # allow creation of new certificates
